import React from "react";

const TemplateSub = ({ children }) => {
    return (
        <main id="contents" className="sub">
            {children}
        </main>
    );
};

export default TemplateSub;
